# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

idea.md

# OSM pre-cache output (scripts/osm-cli.ts); published to the `data`
# branch by CI, never committed to main.
/data/
/data-out/

# Compiled osm-cli sidecar binaries (scripts/build-sidecar.ts); build
# artifacts, rebuilt locally with `npm run build:sidecar` or by CI.
/src-tauri/binaries/
*.bun-build

# render-map.ts output (local map previews)
/test-out/
.aider*

/tmp-src/
